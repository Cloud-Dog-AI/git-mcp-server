# Build

Install dependencies and run the local checks:

```bash
python -m pip install -e .
python -m pytest
```

The public Docker image build is described in [EXTERNAL-BUILD.md](EXTERNAL-BUILD.md).
