# Core runtime
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0.1,<7.0
ruamel.yaml>=0.18.6,<1.0
typing-extensions>=4.10,<5.0

# Git / repo operations
GitPython>=3.1.43,<4.0
dulwich>=0.22.6,<1.0      # optional pure-python backend (useful in constrained envs)

# Diff / merge helpers
deepdiff>=7.0,<8.0
unidiff>=0.7.5,<1.0

# File tooling (branch-scoped workspace file ops)
filelock>=3.15,<4.0
watchdog>=4.0,<5.0
chardet>=5.2,<6.0
python-magic>=0.4.27,<1.0 ; platform_system != "Windows"

# Structured formats (edits + validation)
jsonschema>=4.22,<5.0
lxml>=5.2,<6.0
beautifulsoup4>=4.12,<5.0
markdown-it-py>=3.0,<4.0

# Web/API server (API-first; WebUI calls the API)
fastapi>=0.112,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
python-multipart>=0.0.9,<1.0  # for uploads if needed
itsdangerous>=2.2,<3.0        # session signing (optional)

# AuthN/AuthZ / RBAC
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7.4,<2.0
ldap3>=2.9,<3.0              # optional LDAP integration

# Keycloak / OIDC / SAML (optional)
authlib>=1.3,<2.0
python3-saml>=1.16,<2.0      # optional SAML SP integration

# Secrets / credentials storage
cryptography>=43.0,<44.0
hvac>=2.3,<3.0
keyring>=25.2,<26.0          # optional; OS keychain integration

# Observability
structlog>=24.2,<25.0
rich>=13.7,<14.0

# CLI / admin tooling
typer>=0.12,<1.0

# Testing / quality (dev)
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
