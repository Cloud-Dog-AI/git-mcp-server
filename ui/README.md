# UI Build Output

`ui/dist/` contains the generated Web UI files used by the Docker image.

If you rebuild the Web UI from another source tree, copy the resulting files into `ui/dist/` before building the Docker image.
